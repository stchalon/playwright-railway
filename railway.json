{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "commands": ["pip install -r requirements.txt"]
        }
      }
    }
  },
  "start": "uvicorn app.main:app --host=0.0.0.0 --port=$PORT"
}
